{
    "layer": "top",
    "position": "top",
    "mod": "dock",
    "exclusive": true,
    "height": 44,

    "modules-left": [
        "custom/launcher",
        "hyprland/window"
    ],

    "modules-center": [
        "hyprland/workspaces"
    ],

    "modules-right": [
        "custom/cpu",
        "custom/ram",
        "pulseaudio",
        "network",
        "battery",
        "clock",
        "custom/power"
    ],

    /* ----------------------------------------------------
       Identity / Launcher
       ---------------------------------------------------- */
    "custom/launcher": {
        "format": "\uf303", // Arch logo (Quantum style)
        "on-click": "wofi -show drun",
        "tooltip": "QuantumArch Launcher"
    },

    /* ----------------------------------------------------
       Window Title
       ---------------------------------------------------- */
    "hyprland/window": {
        "format": "{}",
        "max-length": 40
    },

    /* ----------------------------------------------------
       Workspace – macOS Sonoma Dot Style
       ---------------------------------------------------- */
    "hyprland/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "format": "{icon}",
        "format-icons": {
            "default": "\uf111",
            "active": "\uf192"
        }
    },

    /* ----------------------------------------------------
       Developer Performance Metrics (hover reveal)
       ---------------------------------------------------- */
    "custom/cpu": {
        "exec": "~/.config/waybar/scripts/system-info.sh cpu",
        "interval": 3,
        "tooltip": "~/.config/waybar/scripts/system-info.sh cpu-full"
    },

    "custom/ram": {
        "exec": "~/.config/waybar/scripts/system-info.sh ram",
        "interval": 5,
        "tooltip": "~/.config/waybar/scripts/system-info.sh ram-full"
    },

    /* ----------------------------------------------------
       Audio
       ---------------------------------------------------- */
    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": "\uf6a9 Muted",
        "on-click": "pavucontrol",
        "scroll-step": 4,
        "format-icons": {
            "default": ["\uf026", "\uf027", "\uf028"]
        }
    },

    /* ----------------------------------------------------
       Network
       ---------------------------------------------------- */
    "network": {
        "format-wifi": "\uf1eb {signalStrength}%",
        "format-ethernet": "\uf796 Wired",
        "tooltip-format": "{essid}  {ipaddr}",
        "format-disconnected": "\uf071 Offline"
    },

    /* ----------------------------------------------------
       Battery
       ---------------------------------------------------- */
    "battery": {
        "states": { "good": 90, "warning": 30, "critical": 15 },
        "format": "{icon} {capacity}%",
        "format-charging": "\uf0e7 {capacity}%",
        "format-icons": ["\uf244", "\uf243", "\uf242", "\uf241", "\uf240"]
    },

    /* ----------------------------------------------------
       Clock
       ---------------------------------------------------- */
    "clock": {
        "format": "{:%H:%M  \uf017  %a, %d %b}"
    },

    /* ----------------------------------------------------
       Power Menu
       ---------------------------------------------------- */
    "custom/power": {
        "format": "\uf011",
        "on-click": "~/.config/waybar/scripts/power-menu.sh",
        "tooltip": "Power Options"
    }
}
